import styled from 'styled-components';

export const DataListWrapper = styled.section`
  display: flex;
  justify-content: space-evenly;
  flex-wrap: wrap;
  padding: 24px;

  height: 100vh;

  background-color: black;
`;
